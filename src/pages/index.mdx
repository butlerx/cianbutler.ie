import { graphql } from 'gatsby';
import { Home } from '../components/home';

<Home
  title={props.data.site.siteMetadata.title}
  author={props.data.site.siteMetadata.author}
  blurb={props.data.site.siteMetadata.blurb}
  description={props.data.site.siteMetadata.description}
  email={props.data.site.siteMetadata.social.email.address}
  hero={props.data.placeholderImage.childImageSharp.fluid}
  job={props.data.site.siteMetadata.job}
  menu={props.data.site.siteMetadata.menu}
  twitter={props.data.site.siteMetadata.social.twitter.user}
>
### Favorites

> #### Editor: &emsp;<small>vim</small>
>
> #### Operating System: &emsp;<small>Arch Linux</small>
>
> #### Terminal: &emsp;<small>urxvt -> tmux -> zsh</small>

</Home>

export const PageQuery = graphql`
  query IndexQuery {
    site {
      siteMetadata {
        author
        description
        menu
        title
        job
        blurb
        social {
          email {
            address
          }
          twitter {
            user
          }
        }
      }
    }
    placeholderImage: file(relativePath: { eq: "me.png" }) {
      childImageSharp {
        fluid(maxWidth: 800) {
          ...GatsbyImageSharpFluid
        }
      }
    }
  }
`;
